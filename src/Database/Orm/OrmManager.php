<?php

namespace Oak\Database\Orm;

class OrmManager
{
    /*
     * Class responsible for managing ORM models
     * 
     * Some ideas:
     * ------
     * $page = Orm::get(Page::class)->where('id', 1);
     * echo $page->title;
     * ------
     * $page = Page::get(1);
     * echo $page->title;
     * ------
     * $page = Page::where('id', 1)->load();
     * 
     * 
     * */
}